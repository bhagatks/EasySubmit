/**
 * v1 product scope for apply / autofill — single switch for server + extension.
 * Full autofill and one-click apply are v2 per docs/decisions.md.
 */
export const V1_OFFER_AUTOFILL_PHASE = false;
