---
description: Dashboard and landing page UI conventions
globs: src/dashboard/**,src/landing/**,src/shared/BrandLockup.tsx,src/shared/BrandWordmark.tsx
alwaysApply: false
---

# Web UI (Dashboard & Landing)

- React functional components; brand tokens in `src/shared/brand-tokens.css` and `src/landing/landing-tokens.css`.
- Auth/onboarding gate: login → micro-onboarding → workspace (see `FLOW.md`).
- Extension setup prompts live in `src/dashboard/components/ExtensionSetupPrompt.tsx`.
