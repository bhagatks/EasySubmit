/**
 * Standard dashboard content width (768px / 48rem).
 *
 * Wrap every sidebar tab page (Overview, Resume profiles, Job Tracker, AI Keys,
 * Settings, and new dashboard routes) with `DashboardWorkspacePage` or
 * `DashboardWorkspaceShell` using this class — do not pick ad‑hoc max-w-* values.
 */
export const DASHBOARD_WORKSPACE_WIDTH_CLASS = "mx-auto w-full max-w-3xl";
