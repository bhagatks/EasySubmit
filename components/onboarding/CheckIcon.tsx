import { Check } from "lucide-react";

export default function CheckIcon() {
  return (
    <Check
      size={20}
      strokeWidth={2.5}
      className="shrink-0 text-[#12B3D1]"
      aria-hidden
    />
  );
}
